%h1 New user

= request.inspect

= form_for(@user) do |f|
  =# f.error_messages

  .field
    = f.label :email
    = f.text_field :email
 
  = render :partial => 'form', :locals => {:f => f}
    
  .actions
    = f.submit "Sign up"
