<%= link_to vote_char(@wish), add_vote_wish_url(@wish, :subdomain => @wish.issue.permalink) %>

<p>
  <b>Name:</b>
  <%= @wish.name %>
</p>

<p>
  <b>Content:</b>
  <%= @wish.content %>
</p>

<p>
  <b>User:</b>
  <%= @wish.user.email%>
</p>

<p>
  <b>Tag list:</b>
  <%= @wish.tag_list %>
</p>


<%= link_to 'Edit', edit_wish_path(@wish, :subdomain => @wish.issue.permalink) %> |
<%= link_to 'Back', wishes_path %>
<%= link_to 'Destroy', poly_wish_url(@wish), :confirm => 'Are you sure?', :method => :delete %>
